{
    "$id": "spec.json#",
    "title": "Completion Spec",
    "description": "Schema for the overall completion spec",
    "type": "object",
    "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "$recursiveAnchor": true,
        "subcommands": {
            "type": "array",
            "items": {
                "$ref": "command.json#"
            }
        },
        "options": {
            "type": "array",
            "items": {
                "$ref": "option.json#"
            }
        },
        "args": {
            "type": ["object", "array"],
            "$ref": "arg.json#",
            "items": {
                "$ref": "arg.json#"
            }
        },
        "additionalSuggestions": {
            "type": "array",
            "items": {
                "$ref": "suggestion.json#"
            }
        }
    }
}